pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
			    git ''
                sh './mvn clean compile'
				// bat '.\mvnw clean compolie'
				
			 }
         }
     }
}	 
