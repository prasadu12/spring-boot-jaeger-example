pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
	        git 'https://github.com/prasadu12/spring-boot-jaeger-example.git'
                sh './mvn clean compile'
	        // bat '.\mvnw clean compolie'
				
             }
        }
    }
}	
